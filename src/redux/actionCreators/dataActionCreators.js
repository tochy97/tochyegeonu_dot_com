import * as types from "../types/dataTypes";


export const setData = (data) => ({
    type: types.SET_DATA,
    payload: data
})

export const getData = (user) => {

}

export const sendData = (user, data) => async dispatch => {
}